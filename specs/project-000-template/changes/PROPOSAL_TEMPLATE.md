# CH-NNN: <变更标题>

> **说明**：本文件由 `python3 governance/propose.py <project-id> "<变更标题>"` 生成。  
> 填写完毕后，运行 `impact.py` 评估影响范围，再通过 `apply-and-archive.py apply` 确认实施。

## 状态

- **提议时间**：YYYY-MM-DD
- **当前状态**：draft | in-progress | archived
- **变更 ID**：CH-NNN
- **提议人**：<姓名>

---

## 背景

> **为什么要做这个变更？当前痛点是什么？**  
> 描述触发这个变更的原因，可以是：新需求、Bug 修复、技术债偿还、外部约束变化等。

```
例：当前用户注册只支持邮箱+密码，运营反馈有大量用户希望使用手机号注册。
根据数据，30% 的注册流程中断发生在邮箱验证步骤。
```

---

## 变更范围

> **这次变更涉及哪些功能模块、哪些文件**。  
> 要求精确，不要写"可能涉及"，不确定的在 impact.py 运行后填入。

**功能模块**：
- `<模块名>`：<具体变更内容>

**预期涉及文件**：
- `<文件路径>` — <为什么要改>

---

## 预期收益

> **这个变更完成后，用户/系统获得什么**

- **用户侧**：<例：用户可以用手机号注册，注册完成率预计提升 20%>
- **系统侧**：<例：新增手机号唯一性约束，减少账号重复问题>
- **开发侧**：<例：统一认证入口，后续接入微信登录更方便>

---

## 不在范围内

> **明确排除的内容，防止范围蔓延。每个变更都要有这一节。**

- ❌ 不支持手机号登录（仅支持注册，登录在下个变更处理）
- ❌ 不修改现有邮箱注册逻辑
- ❌ <其他明确不做的事>

---

## 风险

> **可能引入的风险和缓解措施**

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|---------|
| 修改 user 表结构影响现有数据 | 中 | 高 | 新增字段设为 nullable，编写数据迁移脚本 |
| <其他风险> | 低/中/高 | 低/中/高 | <缓解措施> |

---

## 关联文档

- **对应 Delta**：`CH-NNN/delta.md`
- **影响评估**：`CH-NNN/impact.md`（运行 impact.py 后生成）
- **相关需求**：REQ-NNN, REQ-NNN
